# iOS 全端修复执行记录

依据用户指定的 [mobile-app-ui-design](https://github.com/ceorkm/mobile-app-ui-design/blob/main/SKILL.md) 及其 industry-conventions 引用。已读取原文；保留原生 SwiftUI、SF Symbols 和动态字体，不套用其 Web 框架示例。用户已经授权直接修改，不增加设计确认关卡。

方向：真实体育工具与社群，白/深色中性底、钴蓝关键操作、清晰赛事及队伍身份。首页优先近期可报名赛事和与用户有关的事项；取消通用宣传口号。五栏：首页、赛事、生涯、视频、我的。生涯明确区分报名、队伍关系、实际出场；不得虚构成绩或成长等级。视频清楚标记官方外部观看与平台直播的区别。

顺序：
1. 修复搜索/个人发布的100条截断、入队之后的入口、撤回重提、消息已读；HTTP + 独立数据库验证。
2. 统一全端布局、文字、按钮、筛选、加载/空/错误/完成状态；补充资料编辑和反馈。
3. 原生构建、模拟器逐页检查、核心操作及深色验证。

现有开发账号与数据不能冒充正式账号与赛事。没有短信服务、正式身份服务和自有直播源时，保留准确的能力边界。

## 回归证据

新增四个数据库/API回归测试，旧代码均失败；修复后18个测试通过。日志在本地 .local/mobile-red.log / mobile-green.log。

## 验收

后续记录实际构建及 UI 操作结果；不以编译成功代替视觉验收。

## 本次已落地

- 五栏导航：首页、赛事、生涯、视频、我的；保留原赛事报名和队伍名单维护。
- 首页取消通用宣传口号，以近期可报名赛事和个人申请入口组织；同步城市选择。
- 赛事详情分为概览、规程、参赛队伍、赛程。参赛队伍读取审核通过的实际报名，不展示私有名单。
- 主办方手动发布/修改/取消对阵、录入比赛总比分；同赛事内队伍/场地冲突与并发版本保护。
- 生涯展示当前队伍赛程、比赛结果、报名和社区参与记录，并明确这些不是个人出场战绩。
- 视频提供经 FAI 官方赛事页验证的 2025 世锦赛回放合集；原生浏览器打开外部来源、本机收藏和搜索。没有伪造直播、预告、播放次数或比赛画面。
- 我管理的和我加入的队伍统一入口；成员查看、负责人移除、本人退出。
- 找队邀请必须选择本人的同级别队伍，接受后建立入队关系；旧版未关联队伍的联系记录仍能保留。
- 申请撤回后可重提，已接受记录不会被重复点击重置；会话保留。
- 服务端条件查询/分页，修复原100条截断；社区列表按条件首批加载并支持“加载更多”，首页首屏只取3条社区动态。搜索防抖与取消保护，个人发布和队伍动态按归属查询。目录和全局搜索仍会分批读取全部匹配记录，超大数据量需进一步按需加载。
- 未读留言隔离、进入会话标记已读、前台自动刷新；不是系统推送。
- 显示名称修改、反馈保存及私人提交记录、浅色/深色/跟随系统设置。
- 固定深色聊天气泡背景色，白字对比度约5.37:1；主页面用统一字体层级和4/8pt间距。
- 截止时间在客户端重新判断，防止缓存报名状态在过期后仍可点击。

## 当前验证结果与边界

- `npm run check`：API和Web构建通过，23项真实PostgreSQL/API测试通过。Web界面没有改动。
- `bash scripts/ios-model-check.sh`：真实iOS模型的截止/可报名/满额判断通过，有旧版失败日志。
- `npm run ios:build`：原生构建通过。
- 模拟器读取先遇到Xcode组件提示，随后明确提示Mac锁屏；不能据此声称新版像素效果与点击流程已经验收。
- 仍未完成：正式身份/监护服务、个人实际出场档案、正式赛制与积分榜、自有直播上传和播放服务、志愿者班次签到、约赛及跨赛事场地/时间冲突、系统推送。这些不是本次已完成能力，不应按上线成品验收。

官方回放来源：https://www-2025.fai.org/wdsc2025-livestream
官方页面提供的合集：https://youtube.com/playlist?list=PLyXQtZ_gjrxDK_6U2JK1KTlFKIckwwJ7i

补充验证：使用实际 Swift APIClient 编译运行只读 smoke，公开目录返回3支队伍，包含中文与 `+ & / #` 的查询参数正确传达并过滤；新包已安装并由 simctl 启动成功。此项仅证明客户端协议和进程启动，不代表界面验收。


## 后续回归证据

- 找队邀请：旧行为缺少队伍仍返回201，新测试期待400而失败；修复后邀请接受建立成员关系。
- 赛程：新增端点前授权测试返回404而失败；实现后覆盖主办方范围、时间冲突、版本冲突、比分范围、当前队伍查询、并发排赛及未来比赛禁止结束。
- 23项测试包含原有14项回归。真实HTTP及隔离PostgreSQL测试不写入本地展示数据；没有伪造赛事/比分来填满新版页面。
- 可重复执行 `bash scripts/ios-api-smoke.sh`，使用实际Swift客户端验证含中文和特殊字符的查询；`bash scripts/ios-model-check.sh` 验证报名状态。
- 本轮仍未完成逐页点击、键盘遮挡、动态文字和深色模式的运行时视觉验收。页面实现和构建通过不等于这些检查通过。

最终本地检查：23/23 API测试通过，格式检查与diff空白检查通过，最新版原生构建成功。新包已安装到iPhone 17 Pro模拟器并启动（进程78194）；本地API已重启到本轮实现。仍不能把启动成功视为视觉或点击验收。


## 2026-09-08 模拟器实机画面复核

环境：iPhone 17 Pro / iOS 26.4。用户解锁后，通过 CUA 实际读取 Simulator 页面和执行点击，不再以进程启动代替界面证据。

### 本轮发现并修复

1. 首页工具栏的 Label 被系统收缩为定位图标，城市名不可见。改为显式图标与文字；已看到“全国”“杭州”，选择杭州后只显示杭州赛事。
2. 首页及全局搜索的赛事行只有日期，没有月份，跨月结果容易误读。仅为此处赛事行增加月份；赛事页按月份分组的布局保持原样。已在“赛”的搜索结果中看到10月11/18/24日。
3. 深色下部分突出按钮继承浅蓝 accent，出现浅蓝底白字。统一为固定钴蓝底；已复核招募页按钮，其他同类操作使用同一颜色。
4. 空招募表单可以点击发布，缺项提示要提交后才出现。改为复用一处客户端校验来禁用按钮和显示具体缺项提示；补齐长度及时间检查，服务端校验仍保留。实际看到空表单禁用、选队缺失提示、缺少详细说明提示。

### 已执行的界面检查

- 五栏实际页面：主页、赛事、生涯、视频、我的。
- 本地服务停止时错误状态可见；服务恢复后首页显示真实本地数据。
- 全局搜索、城市切换、赛事月份与队伍/成员入口。
- 从赛事“登录后报名”进入演示登录，成功后自动打开报名表；选择队伍显示保存的名单，未确认规则时禁用提交，确认后启用。未提交新的报名。
- 队伍详情→社区成员可达，显示负责人；社区关系与报名名单说明可见。此次未实际移除成员。
- 视频收藏→我的收藏→取消收藏显示空状态，恢复了原来的未收藏状态。此轮未验证外部YouTube视频播放。
- 主办方登录→本机构赛事→查看赛程→发布表单；不足两支已审核队伍时显示原因并禁用保存。没有提交虚构对阵或比分。
- 深色设置立即生效；在生涯、首页、社区、设置、赛程表单检查文字与操作。模拟器文字提高两档后检查布局；不等于已覆盖最大辅助功能字号和所有设备尺寸。
- 软件键盘出现时检查表单输入区域和取消入口。CUA对多行TextField的setValue会出现画面值与SwiftUI绑定不一致，粘贴也超时；因此没有把完整招募草稿提交标为UI通过，保留后端原有持久化测试证据。

最新原生构建成功（`.local/ios-visual-fixes-build.log`）、模型回归通过、diff检查通过。此轮未修改API；沿用前轮23项API测试结果，没有把未重跑的API测试记作本轮新结果。

结束状态：新版已安装；停留首页，城市恢复全国，外观恢复跟随系统，文字大小回退两档。当前为青空赛事运营演示账号，便于查看主办方赛程入口。未发布验收草稿。

仍需单独验证：完整社区双账号提交/接受/留言/退出UI流程，外部视频播放，最大辅助功能字号，以及不同屏幕尺寸。正式账号、个人出场档案、平台直播等产品能力边界不因本轮视觉复核而改变。
